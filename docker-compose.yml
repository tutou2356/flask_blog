version: "3.8"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    volumes:
      - db_data:/app/instance
      - ./blog.db:/app/blog.db
    restart: unless-stopped

volumes:
  db_data:
