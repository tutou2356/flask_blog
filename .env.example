# Flask 运行环境: development / production
FLASK_ENV=development

# 安全密钥 (生产环境必须设置为随机字符串)
# 可用以下命令生成: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev

# 数据库连接地址 (默认使用 SQLite)
# DATABASE_URI=sqlite:///blog.db
# DATABASE_URI=postgresql://user:pass@localhost:5432/blogdb
